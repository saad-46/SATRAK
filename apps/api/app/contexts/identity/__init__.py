"""Identity bounded context — user profiles, roles, permissions.

Models the *shapes* of RBAC (reusable role/permission/profile entities).
Authentication, SSO, and token issuance are a later epic; this context provides
the domain vocabulary they build on.
"""
