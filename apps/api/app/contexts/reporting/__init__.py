"""Reporting bounded context — structured report generation and exports.

Reserved bounded-context boundary. The domain model, application services,
and persistence for this context are implemented in its dedicated epic; it is
created now so imports, ownership, and CI wiring have a stable home and the
dependency rules are established from day one.
"""
