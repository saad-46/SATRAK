# @satrak/config

Shared, versioned developer configuration for the monorepo — ESLint preset and a
TypeScript base — so every package lints and type-checks against one source of
truth instead of drifting copies.

- `@satrak/config/eslint` — flat-config preset
- `@satrak/config/tsconfig` — TypeScript base (re-exports the repo-root base)
