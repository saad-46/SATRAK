/**
 * @satrak/sdk — typed API client.
 *
 * Placeholder. This package will be GENERATED from each backend service's
 * OpenAPI spec (via openapi-typescript) rather than hand-written, then consumed
 * by apps/web and apps/mobile. Until codegen is wired up (see blueprint), the
 * web app uses its local lib/api client.
 */
export const SDK_PACKAGE_VERSION = "0.1.0";
