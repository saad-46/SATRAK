# @satrak/ui

Shared design-system component library (shadcn/ui primitives + patterns + design
tokens). **Placeholder** — components live in `apps/web` until a second consumer
(citizen portal) justifies extraction here, per the blueprint.
