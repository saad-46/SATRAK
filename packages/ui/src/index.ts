/**
 * @satrak/ui — shared component library (design system).
 *
 * Placeholder at the foundation stage. The shadcn/ui-based primitives currently
 * live in apps/web; they migrate here once a second consumer (the citizen
 * portal) exists, at which point this package becomes the single source of truth
 * for components + design tokens. See ENGINEERING_BLUEPRINT Part 1.
 */
export const UI_PACKAGE_VERSION = "0.1.0";
