# scripts/

Operational and developer helper scripts.

| Script         | Purpose                                                            |
| -------------- | ------------------------------------------------------------------ |
| `dev-setup.sh` | One-shot local bootstrap: seed env files, install JS + Python deps |

Recurring/operational scripts (data backfills, jurisdiction onboarding, model
rollout) are added here as the platform grows, per the blueprint.
